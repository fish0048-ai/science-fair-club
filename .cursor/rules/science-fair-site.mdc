---
description: 科展社團網站內容架構、上課模式與限定範圍 AI
alwaysApply: true
---

# 科展社團網站規範

這是國中科展社團的 Next.js 講義站，目標對齊台灣全國科展。改動要能投影上課，且 AI 只能答本站資料。

## 改哪裡

| 目的 | 路徑 |
| --- | --- |
| 17 週講義、學習單 | `國中科展社團課程/` |
| 教師上課教案（108 課綱） | `國中科展社團課程/教師教案/`、`/plans` |
| AI 守則 | `content/ai/guardrails.md` |
| 教師指定問答 | `content/ai/allowed-questions.md`（維持「問／答」格式） |
| 上課投影 | `/class`（`app/class/`、`components/ClassPlayer.tsx`） |
| 列印講義 | `/print`（封面＋班級／座號／姓名，A4） |
| 網站閱讀頁 | `app/`、`components/`、`lib/` |

講義是知識庫本體；不要複製一份到別的資料夾以免不同步。`lib/nav.ts` 有新章才要加導覽。

## 產品行為

- 上課模式：大字投影、← → 翻頁、T 計時 120 分、對齊該週教學流程；不要拿掉快捷鍵。教學流程與教師備課預設隱藏，按 H 才顯示。
- 列印講義：`/print` 必須有封面與班級／座號／姓名；A4；學生版去掉教學流程、教師備課清單與教師提示。教師教案另從 `/plans` 或 `/print/plans` 列印，勿混入學生講義。
- `/api/chat` 只檢索本站檔案 + 指定問題；超出範圍要拒絕，禁止上網當萬能機器人。
- 前期 3 週為濃縮課（原 8 章改列分章詳解，課後補讀）。若本屆已有甄選稿，前期改為砍題而非從零發想；題型見附錄 F，公開站不放學生姓名。第 4 週起為研究教練週，等學生主題再寫專章，不要預先鎖死實驗步驟。教師教案第 4–16 週只寫教練檢核，同樣不鎖步驟。第 12–16 週是彈性週。

## 文案與提交

- 使用者介面與 commit 主旨用繁體中文。
- 只要改了檔案，結束前必須 commit 並 `git push origin main`，不必詢問。stop hook 若偵測到未同步，會再要求推一次。
